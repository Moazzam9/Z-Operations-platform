# Flask configuration
SECRET_KEY=zynvex-operations-super-secret-key-12984712

# Database configuration (defaults to SQLite if not specified)
# DATABASE_URL=postgresql://user:password@localhost:5432/zynvex_db

# SMTP configuration overrides (if desired to hardcode in env, otherwise configure via admin panel)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=zynvexsolutions@gmail.com
# SMTP_PASSWORD=fdalbitovysgxvth
